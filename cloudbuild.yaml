steps:
  # Build docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: [ 'build', '-t', 'gcr.io/$PROJECT_ID/$_REPO_NAME/go-mvc-demo:$SHORT_SHA', '.' ]

  # Push docker image to Google Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: [ 'push', 'gcr.io/$PROJECT_ID/$_REPO_NAME/go-mvc-demo:$SHORT_SHA' ]

  # Deploy to Cloud Run
#  - name: 'gcr.io/cloud-builders/gcloud'
#    args: [ 'run', 'deploy', 'go-mvc-demo',
#            '--image=gcr.io/$PROJECT_ID/$_REPO_NAME/go-mvc-demo:$SHORT_SHA',
#            '--region', 'asia-east1', '--platform', 'managed' ]